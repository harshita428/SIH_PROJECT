# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some Oxlint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Oxc](https://oxc.rs)
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/)

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the Oxlint configuration

If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for information on how to integrate TypeScript and Oxlint's TypeScript related rules in your project.
### 🚀 How to Run **KALACHAKRA**

#### 1. View It Right Now (Already Running!)
The local development server is **already active and running** for you in the background. You can open it immediately:

👉 **[http://localhost:5173/](http://localhost:5173/)** in your web browser.

---

#### 2. Running in Your Terminal (Anytime)
Whenever you open a new terminal (in VS Code or PowerShell) inside this project folder:

```powershell
# 1. Navigate to the project folder (if not already there)
cd "c:\Users\Harshita\OneDrive\ドキュメント\SIH"

# 2. Start the local development server
npm run dev
```

Once started, click or open `http://localhost:5173` in your browser.

---

#### 3. Other Useful Commands

- **Build for Production** (creates an optimized `dist/` folder):
  ```powershell
  npm run build
  ```

- **Preview the Production Build**:
  ```powershell
  npm run preview
  ```

- **Install Dependencies** (if cloning on another machine):
  ```powershell
  npm install
  ```

---

#### 💡 Key Shortcuts to Try on the Live Site:
- Press <kbd>Ctrl</kbd> + <kbd>K</kbd> anywhere on the page to launch the **Instant Heritage Search Modal**.
- Click **"Start Your Journey"** or any era pill to travel through the 6 historical epochs.
- Click on any monument card (like *Konark Sun Temple* or *Kailasa Temple*) to open the **Architectural Blueprint & Audio Guide Modal**.
